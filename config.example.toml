# apple-to-last-fm configuration
#
# You don't need to create this manually.
# Run `apple-to-last-fm auth` and it will be created automatically.
#
# The only field you might want to set manually:

# poll_interval_secs = 10
